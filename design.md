# ARIGATO TiP — ロール設計 / Role Design

> 作成日: 2026-07-30 / 参照: Slack #proj-arigato-tip 外部パートナー（thread 1785395322）
> 現状は「管理者」ロールのみ。クライアント要望により「店舗運営者」ロールを追加する。

---

## 1. ロール定義

| ロール | 範囲 | アカウント | 例 |
|---|---|---|---|
| **管理者 (Platform Admin)** — 運営元 | **全店舗**（横断・集計・各店舗） | 運営元のアカウント | 私たち（プラットフォーム運営） |
| **店舗運営者 (Store Operator)** — 店舗運営社 | **自店舗のみ** | **1 店舗 = 1 アカウント**（オーナーが複数店舗でも、店舗ごとに別アカウント発行） | カフェARIGATO（店舗ID: 12345）の店長 |

**基本ルール**
- 管理者は全店舗を管理できる。**店舗ごとに「追加 / 削除 / 停止（一時停止）」できる専用ページ**を持つ。
- 店舗運営者は自店舗のみ。**自分の店舗に対して、現行プロジェクトと同様に「店舗情報・QR・STORYページ」などを編集できる。** STORY・QR・店舗設定などは各店舗が自分で行う。
- 店舗運営者には「店舗切替（すべて / 店舗別）」を出さない（自店舗に固定）。

---

## 2. ページ別アクセス権（既存ページ）

現在実装済みのページに対する、ロール別のアクセス範囲。

| ページ | ルート | 管理者（全店舗） | 店舗運営者（自店舗） |
|---|---|---|---|
| ダッシュボード | `/admin` | ✅ 全店舗 + 店舗切替 | ✅ 自店舗のみ |
| 通知 | `/admin/notifications` | ✅ 全店舗 | ✅ 自店舗のみ |
| チップ履歴 | `/admin/tips` | ✅ 全店舗 | ✅ 自店舗のみ |
| 口コミ一覧 | `/admin/reviews` | ✅ 全店舗 | ✅ 自店舗のみ |
| レポート | `/admin/reports` | ✅ 全店舗 + 店舗別 | ✅ 自店舗のみ |
| 店舗管理（一覧・**新規作成**・**削除**・**停止（一時停止）**・任意店舗の編集） | `/admin/stores` | ✅ フル | ❌ 非表示（複数店舗一覧は持たない） |
| └ 店舗編集 + 店舗情報 / QR / STORY | `/admin/stores/[id]` | ✅ 任意店舗 | ✅ **自店舗のみ**（「店舗設定」として、現行と同様に編集可） |
| 設定 | `/admin/settings` | ✅ | ✅ 自アカウント / 自店舗 |
| **店舗切替（すべて / 店舗別）** | トップバー | ✅ 表示 | ❌ 非表示（自店舗に固定） |

---

## 3. 新規ページ（モック提案 / 未実装・フェーズ2候補）

クライアントのモック（店舗運営者パネル）にあるが、現状は未実装のもの。

| ページ | 対象 | 補足 |
|---|---|---|
| QRコード（専用ページ） | 店舗運営者 | 現状は店舗編集画面の中にQRがある。モックは独立ページを希望 |
| 売上・振込管理 | 店舗運営者（+ 管理者） | **新機能** — 振込 / 精算。現状に相当機能なし |
| お知らせ | 両方 | 運営 → 店舗へのお知らせ。新規 |
| ヘルプ・よくある質問 | 両方 | 静的FAQ。新規 |
| 運営へのお問い合わせ（チャット） | 店舗運営者 ⇄ 管理者 | モックの中心画面。ただし Slack reply 7 で「チャットツールが優先ではなく、店舗運営者の管理画面が必要か」との整理 → **後回し推奨** |
| スタッフ管理 | — | モックに残っているが、ストーリー先行のピボットで**スタッフ機能は削除済み**。実装前に要確認 |

---

## 4. データモデル / 実装方針

現状: `AdminUser` に `role String @default("owner")` と任意の `storeId` あり。単一管理者が店舗切替で全店舗を見る構成。

追加で必要なこと:

1. **ロール区分**: `AdminUser.role` を `admin`（運営元）/ `store_operator`（店舗運営者）で運用。
2. **スコープの強制**: 店舗運営者のときは全クエリを本人の `storeId` に限定（`store-scope` を role 判定で分岐）。
3. **1 アカウント = 1 店舗**: 招待 / サインアップ時に店舗を紐付け、`storeId` 必須（店舗運営者の場合）。
4. **店舗の停止（suspend）**: `Store.status`（`active` / `suspended`）を追加。
   - 管理者は店舗管理ページから店舗を「停止 / 再開」できる。
   - **停止中の店舗はゲストのチップページ（`/s/[slug]`）を無効化**（受付停止の表示、新規チップ・口コミを受け付けない）。集計・履歴データは保持する。
   - 停止中は店舗運営者のログインを許可するか（閲覧のみ）等の扱いは要確認。
5. **UIの出し分け**:
   - 店舗運営者には店舗切替と「店舗管理（一覧・新規・削除・停止）」を非表示。
   - 店舗編集 / 店舗情報 / STORY / QR は「自店舗設定」として本人の店舗のみ編集可。
6. **アクセス制御**: ルート単位でロール＋店舗所有をサーバー側で検証（他店舗IDへの直アクセスを拒否）。

---

## 5. フェーズ分け提案

- **Phase 1（推奨・先行）**: 既存ページの上にロール分割を載せる。
  - 管理者 = 全店舗、店舗運営者 = 自店舗のみ。
  - 店舗切替 / 店舗管理の出し分け、クエリのスコープ、1アカウント=1店舗。
  - **店舗の停止 / 再開**（`Store.status` + 停止中はゲストページ無効化）。
  - 店舗運営者による自店舗の編集（店舗情報・QR・STORY）＝ 現行機能をロールでスコープ。
- **Phase 2（新機能）**: 売上・振込管理 / お知らせ / ヘルプ / 運営への問い合わせチャット。
  - いずれも新規機能のため、デザイン確定後に個別に実装。

---

## 6. 確認事項（Open questions）

- **スタッフ管理**: モックに残るが no-staff 仕様と矛盾。店舗運営者ロールで必要か要確認。
- **売上・振込管理**: 表示のみか、実際の振込フロー（Stripe payout 等）まで含むか。
- **問い合わせチャット**: 優先度。メール / 公式LINE などの代替で足りるか（Slack reply 6）。
- **既存 `role: "owner"`** の扱い（`admin` に読み替えるか、マイグレーションするか）。

---

> 全体のロジック更新が必要。デザインは今週末に作成予定（Slack reply 13）。デザイン確定後、Phase 1 から着手する。
